import java.util.*

val scanner = Scanner(System.`in`)

fun main() {
    var ntimes = 0
    do {
        val x = scanner.nextLine()
        if (x == "0") break
        ntimes ++
    } while (true)

    println(ntimes)
}